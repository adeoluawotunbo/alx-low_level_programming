#include <stdio.h>
/**
 *main -prints exactly the statement, followed by the new line
 *Return: 0 if exited properly, non-zero
*/
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
