#include "holberton.h"

/**
 * mul - Multiplies 2 integers
 * @a: the first integer value to be multiplied
 * @b: the second integer value to be multiplied
 * Return: the value gotten from multiplying a and b together
 */

int mul(int a, int b)
{
return (a * b);
}
